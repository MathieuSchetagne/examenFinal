<!DOCTYPE html>
<html>

        <%-  include head %>

        <%-  include entete %>
    <body>

<div>
        <% for (p in adresses) {%>
            <h3><%= adresses[p] %></h3>
        <% } %>
 
</div>

</body>

<%-  include footer %>

</html>